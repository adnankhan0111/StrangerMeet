<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- ✅ TITLE: Primary keyword first, 58 chars -->
  <title>Chat with Strangers Free – Random Video & Text Chat | ChatWithStrangers.fun</title>

  <!-- ✅ META DESCRIPTION: 155 chars, primary + secondary keywords + CTA -->
  <meta name="description" content="Chat with strangers free — no sign up, no download. Instant random video chat & anonymous text chat with people worldwide. Best Omegle alternative 2026. Start in 5 seconds!">

  <!-- ✅ Robots + indexing signals -->
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1">
  <meta name="googlebot" content="index, follow">
  <meta name="author" content="ChatWithStrangers.fun">
  <meta name="theme-color" content="#0f0c29">

  <!-- ✅ Last Modified (updated to today) -->
  <meta name="last-modified" content="2026-06-17">

  <!-- ✅ Canonical -->
  <link rel="canonical" href="https://chatwithstrangers.fun/">

  <!-- ✅ Open Graph -->
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://chatwithstrangers.fun/">
  <meta property="og:title" content="Chat with Strangers Free – Random Video & Text Chat">
  <meta property="og:description" content="Instant random video chat & anonymous text chat with strangers worldwide. No sign up. No downloads. Best Omegle alternative 2026.">
  <meta property="og:image" content="https://chatwithstrangers.fun/og-image.jpg">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:site_name" content="ChatWithStrangers.fun">
  <meta property="og:locale" content="en_US">

  <!-- ✅ Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Chat with Strangers Free – Random Video & Text Chat">
  <meta name="twitter:description" content="Instant random video & anonymous text chat. No sign up. Best Omegle alternative 2026.">
  <meta name="twitter:image" content="https://chatwithstrangers.fun/og-image.jpg">

  <!-- ✅ Bing Webmaster -->
  <meta name="msvalidate.01" content="4A4FE642528A43FE3D09BB35CE461E4D">

  <!-- ✅ Favicons -->
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="icon" type="image/png" sizes="32x32" href="/logo.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/logo.png">
  <link rel="manifest" href="/site.webmanifest">

  <!-- ✅ Preconnect for fonts only (critical) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

  <!-- ✅ Font: Inter (system-level speed) + Space Grotesk (display) — non-blocking -->
  <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=Inter:wght@400;500;600&display=swap" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=Inter:wght@400;500;600&display=swap"></noscript>

  <!-- ✅ Font Awesome: async, non-blocking -->
  <link rel="preload" as="style" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" onload="this.onload=null;this.rel='stylesheet'">
  <noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"></noscript>

  <!-- ✅ SCHEMA: Full graph — Organization, WebSite, WebPage, SoftwareApplication, FAQPage, BreadcrumbList -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Organization",
        "@id": "https://chatwithstrangers.fun/#organization",
        "name": "ChatWithStrangers.fun",
        "url": "https://chatwithstrangers.fun",
        "logo": {
          "@type": "ImageObject",
          "url": "https://chatwithstrangers.fun/logo.png",
          "width": 200, "height": 60
        },
        "sameAs": ["https://www.youtube.com/@ChatWithStrangersfun"]
      },
      {
        "@type": "WebSite",
        "@id": "https://chatwithstrangers.fun/#website",
        "url": "https://chatwithstrangers.fun",
        "name": "ChatWithStrangers – Free Random Video & Text Chat",
        "description": "Chat with strangers online free. Random video chat and anonymous text chat with people worldwide. No sign up required.",
        "publisher": {"@id": "https://chatwithstrangers.fun/#organization"},
        "inLanguage": "en-US",
        "potentialAction": {
          "@type": "SearchAction",
          "target": "https://chatwithstrangers.fun/?s={search_term_string}",
          "query-input": "required name=search_term_string"
        }
      },
      {
        "@type": "WebPage",
        "@id": "https://chatwithstrangers.fun/#webpage",
        "url": "https://chatwithstrangers.fun",
        "name": "Chat with Strangers Free – Random Video & Text Chat | ChatWithStrangers.fun",
        "description": "Chat with strangers free — no sign up, no download. Instant random video chat & anonymous text chat worldwide. Best Omegle alternative 2026.",
        "isPartOf": {"@id": "https://chatwithstrangers.fun/#website"},
        "publisher": {"@id": "https://chatwithstrangers.fun/#organization"},
        "dateModified": "2026-06-17",
        "datePublished": "2024-01-01",
        "breadcrumb": {"@id": "https://chatwithstrangers.fun/#breadcrumb"},
        "speakable": {
          "@type": "SpeakableSpecification",
          "cssSelector": ["h1", ".hero-sub"]
        }
      },
      {
        "@type": "BreadcrumbList",
        "@id": "https://chatwithstrangers.fun/#breadcrumb",
        "itemListElement": [
          {"@type": "ListItem", "position": 1, "name": "Home", "item": "https://chatwithstrangers.fun/"}
        ]
      },
      {
        "@type": "SoftwareApplication",
        "@id": "https://chatwithstrangers.fun/#app",
        "name": "ChatWithStrangers",
        "applicationCategory": "CommunicationApplication",
        "operatingSystem": "Web Browser, iOS, Android",
        "offers": {"@type": "Offer", "price": "0", "priceCurrency": "USD"},
        "aggregateRating": {
          "@type": "AggregateRating",
          "ratingValue": "4.8",
          "reviewCount": "12400",
          "bestRating": "5"
        },
        "description": "Free random video chat and anonymous text chat with strangers worldwide. No sign up required.",
        "url": "https://chatwithstrangers.fun",
        "featureList": ["Random video chat", "Anonymous text chat", "No registration", "190+ countries", "Instant matching", "Mobile-friendly"]
      },
      {
        "@type": "FAQPage",
        "mainEntity": [
          {
            "@type": "Question",
            "name": "Is ChatWithStrangers completely free?",
            "acceptedAnswer": {"@type": "Answer", "text": "Yes — 100% free forever. No hidden charges, subscriptions, credits, or premium tiers. Random video chat, anonymous text chat, filters, and instant matching are all free."}
          },
          {
            "@type": "Question",
            "name": "Do I need to register to chat with strangers?",
            "acceptedAnswer": {"@type": "Answer", "text": "No registration needed. No email, no phone number, no personal data required. Open the site, click Start, and you're connected with a random stranger in seconds."}
          },
          {
            "@type": "Question",
            "name": "Is ChatWithStrangers the best Omegle alternative in 2026?",
            "acceptedAnswer": {"@type": "Answer", "text": "Yes — it's one of the best Omegle alternatives in 2026. After Omegle shut down, ChatWithStrangers became the most trusted platform for random stranger chat with faster matching, stronger safety, and better privacy."}
          },
          {
            "@type": "Question",
            "name": "Is it safe to talk with strangers online here?",
            "acceptedAnswer": {"@type": "Answer", "text": "Yes. We use 24/7 content moderation, one-click skip and report, HTTPS encryption, and collect zero personal data. You never need to share your name, email, or location."}
          },
          {
            "@type": "Question",
            "name": "Does it work on mobile phones?",
            "acceptedAnswer": {"@type": "Answer", "text": "Yes, perfectly. Works on all modern iOS and Android devices directly in your browser. No app download required."}
          },
          {
            "@type": "Question",
            "name": "Can I use both video chat and text chat?",
            "acceptedAnswer": {"@type": "Answer", "text": "Yes. Switch freely between random video chat and anonymous text chat with no restrictions on any device."}
          },
          {
            "@type": "Question",
            "name": "Why do people like talking with strangers online?",
            "acceptedAnswer": {"@type": "Answer", "text": "Random stranger chat offers spontaneous, judgment-free conversation. People use it to fight boredom, improve social confidence, practice languages, or enjoy unexpected connections you can't find on social media."}
          },
          {
            "@type": "Question",
            "name": "How many people are online on ChatWithStrangers right now?",
            "acceptedAnswer": {"@type": "Answer", "text": "Thousands of users from 190+ countries are active at any moment. You're never more than seconds away from meeting someone new."}
          }
        ]
      }
    ]
  }
  </script>

  <!-- ✅ CRITICAL CSS — fully inlined, zero render-blocking -->
  <style>
    /* === DESIGN TOKENS === */
    :root {
      --bg:        #0a0a0f;
      --bg2:       #111118;
      --surface:   rgba(255,255,255,0.045);
      --border:    rgba(255,255,255,0.08);
      --border-h:  rgba(255,255,255,0.16);
      --accent:    #7c6dfa;
      --accent2:   #a78bfa;
      --accent3:   #38d9a9;
      --text:      #f1f0f5;
      --muted:     rgba(241,240,245,0.55);
      --display:   'Space Grotesk', system-ui, sans-serif;
      --body:      'Inter', system-ui, sans-serif;
      --radius:    16px;
      --radius-sm: 10px;
      --glow:      0 0 40px rgba(124,109,250,0.18);
    }

    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; -webkit-text-size-adjust: 100%; }

    body {
      font-family: var(--body);
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
      line-height: 1.6;
    }

    /* === NOISE TEXTURE OVERLAY (GPU layer, zero reflow) === */
    body::before {
      content: '';
      position: fixed; inset: 0; z-index: 0; pointer-events: none;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.035'/%3E%3C/svg%3E");
      opacity: 0.4;
    }

    /* === AMBIENT GLOW ORBS === */
    .orbs { position: fixed; inset: 0; pointer-events: none; z-index: 0; overflow: hidden; }
    .orb {
      position: absolute; border-radius: 50%;
      filter: blur(80px); will-change: transform;
      animation: drift 20s infinite ease-in-out;
    }
    .orb-1 { width: 500px; height: 500px; background: rgba(124,109,250,0.12); top: -10%; left: -8%; }
    .orb-2 { width: 600px; height: 600px; background: rgba(56,217,169,0.07); bottom: -15%; right: -10%; animation-delay: -8s; }
    .orb-3 { width: 300px; height: 300px; background: rgba(167,139,250,0.09); top: 40%; right: 20%; animation-delay: -14s; }
    @keyframes drift {
      0%,100% { transform: translate(0,0); }
      33% { transform: translate(20px,-30px); }
      66% { transform: translate(-15px,20px); }
    }

    /* === NAVIGATION === */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      height: 64px;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 clamp(1rem, 4vw, 2.5rem);
      background: rgba(10,10,15,0.7);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
      transition: background 0.3s;
    }
    nav.solid { background: rgba(10,10,15,0.96); }

    .nav-logo { display: flex; align-items: center; gap: 10px; text-decoration: none; }
    .nav-logo img { width: 110px; height: 36px; object-fit: contain; }
    .nav-wordmark {
      font-family: var(--display); font-weight: 700; font-size: 1rem;
      color: var(--text); letter-spacing: -0.3px;
    }
    .nav-wordmark span { color: var(--accent2); }

    .nav-links { display: flex; list-style: none; gap: 0.2rem; align-items: center; }
    .nav-links a {
      font-family: var(--body); font-size: 0.875rem; font-weight: 500;
      color: var(--muted); text-decoration: none;
      padding: 0.4rem 0.9rem; border-radius: 8px;
      transition: color 0.2s, background 0.2s;
    }
    .nav-links a:hover { color: var(--text); background: var(--surface); }
    .nav-links a.active { color: var(--accent2); }

    .nav-cta {
      background: var(--accent); color: #fff !important;
      padding: 0.45rem 1.1rem !important; border-radius: 8px !important;
      font-weight: 600 !important; transition: opacity 0.2s, transform 0.2s !important;
    }
    .nav-cta:hover { opacity: 0.88; transform: translateY(-1px); background: var(--accent) !important; }

    .mobile-btn {
      display: none; background: none; border: none;
      color: var(--text); cursor: pointer; padding: 6px; font-size: 1.3rem;
    }

    /* === LAYOUT === */
    .wrap {
      position: relative; z-index: 1;
      width: 100%; max-width: 1160px;
      margin: 0 auto; padding: 0 clamp(1rem, 4vw, 2rem);
    }

    /* === HERO === */
    .hero {
      min-height: 100vh;
      display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      text-align: center;
      padding: 100px 1rem 60px;
      position: relative; z-index: 1;
    }

    .hero-eyebrow {
      display: inline-flex; align-items: center; gap: 8px;
      font-size: 0.78rem; font-weight: 600; letter-spacing: 0.08em;
      text-transform: uppercase; color: var(--accent2);
      background: rgba(124,109,250,0.1);
      border: 1px solid rgba(124,109,250,0.25);
      padding: 6px 16px; border-radius: 50px;
      margin-bottom: 1.5rem;
      animation: fadeUp 0.5s ease both;
    }
    .live-dot {
      width: 7px; height: 7px; background: var(--accent3); border-radius: 50%;
      animation: blink 1.4s infinite;
    }

    h1 {
      font-family: var(--display);
      font-size: clamp(2rem, 5.5vw, 3.6rem);
      font-weight: 700; line-height: 1.15;
      letter-spacing: -0.02em;
      max-width: 820px;
      animation: fadeUp 0.6s 0.1s ease both;
    }

    /* Signature element: the gradient split headline */
    h1 .line-plain { color: var(--text); display: block; }
    h1 .line-grad {
      display: block;
      background: linear-gradient(95deg, var(--accent2) 0%, var(--accent3) 100%);
      -webkit-background-clip: text; background-clip: text; color: transparent;
    }

    .hero-sub {
      font-size: clamp(0.95rem, 2.2vw, 1.1rem);
      color: var(--muted); max-width: 640px;
      margin: 1.4rem auto 0; line-height: 1.8;
      animation: fadeUp 0.6s 0.2s ease both;
    }

    .hero-updated { font-size: 0.75rem; color: var(--muted); margin: 0.8rem 0 0; opacity: 0.55; }

    /* === TRUST PILLS === */
    .trust-row {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 8px;
      margin: 2rem 0;
      animation: fadeUp 0.6s 0.3s ease both;
    }
    .pill {
      display: inline-flex; align-items: center; gap: 6px;
      padding: 6px 14px; border-radius: 50px; font-size: 0.8rem; font-weight: 500;
      background: var(--surface); border: 1px solid var(--border); color: var(--text);
    }
    .pill .dot-green { width: 6px; height: 6px; border-radius: 50%; background: var(--accent3); flex-shrink: 0; }

    /* === CTA BUTTONS === */
    .cta-group {
      display: flex; flex-wrap: wrap; justify-content: center; gap: 12px;
      animation: fadeUp 0.6s 0.4s ease both;
    }
    .btn {
      display: inline-flex; align-items: center; gap: 8px;
      font-family: var(--body); font-weight: 600; font-size: 1rem;
      padding: 15px 34px; border-radius: 12px; border: none;
      cursor: pointer; text-decoration: none; position: relative; overflow: hidden;
      transition: transform 0.25s cubic-bezier(0.34,1.56,0.64,1), box-shadow 0.25s;
    }
    .btn:hover { transform: translateY(-3px); }
    .btn-primary {
      background: var(--accent); color: #fff;
      box-shadow: 0 0 0 0 rgba(124,109,250,0.4);
      animation: pulse-glow 2.8s infinite;
    }
    .btn-primary:hover { box-shadow: 0 12px 32px rgba(124,109,250,0.45); }
    .btn-ghost {
      background: var(--surface); color: var(--text);
      border: 1px solid var(--border);
      backdrop-filter: blur(8px);
    }
    .btn-ghost:hover { border-color: var(--border-h); background: rgba(255,255,255,0.07); }

    /* shimmer on primary btn */
    .btn-primary::after {
      content: ''; position: absolute;
      top: 0; left: -100%; width: 100%; height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.15), transparent);
      transition: left 0.5s;
    }
    .btn-primary:hover::after { left: 100%; }

    /* === SOCIAL PROOF NUMBER === */
    .hero-proof {
      margin-top: 2.5rem; font-size: 0.82rem; color: var(--muted);
      animation: fadeUp 0.6s 0.5s ease both;
    }
    .hero-proof strong { color: var(--accent3); }

    /* ==================== SECTIONS ==================== */
    section { position: relative; z-index: 1; }

    .sec {
      padding: clamp(3rem, 7vw, 5.5rem) 0;
    }

    .sec-label {
      font-size: 0.72rem; font-weight: 700; letter-spacing: 0.1em;
      text-transform: uppercase; color: var(--accent2);
      margin-bottom: 0.75rem; display: block;
    }
    .sec-title {
      font-family: var(--display); font-weight: 700;
      font-size: clamp(1.5rem, 3.5vw, 2.2rem);
      line-height: 1.2; letter-spacing: -0.02em;
      margin-bottom: 0.9rem;
    }
    .sec-body {
      font-size: 0.97rem; color: var(--muted); line-height: 1.8;
      max-width: 620px;
    }
    .sec-body a { color: var(--accent2); text-decoration: underline; }
    .centered { text-align: center; }
    .centered .sec-body { margin: 0 auto; }

    /* === DIVIDER === */
    .divider {
      height: 1px; background: linear-gradient(90deg, transparent, var(--border), transparent);
      margin: 0;
    }

    /* === CARDS === */
    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: clamp(1.4rem, 3vw, 2rem);
      transition: border-color 0.25s, box-shadow 0.25s, transform 0.25s;
    }
    .card:hover {
      border-color: var(--border-h);
      box-shadow: var(--glow);
      transform: translateY(-4px);
    }

    /* === MODES GRID (hero below) === */
    .modes-grid {
      display: grid; grid-template-columns: 1fr 1fr; gap: 1.2rem;
      margin-top: 3rem;
    }
    .mode-card { display: flex; gap: 1rem; align-items: flex-start; }
    .mode-icon {
      width: 46px; height: 46px; flex-shrink: 0;
      background: rgba(124,109,250,0.12); border: 1px solid rgba(124,109,250,0.2);
      border-radius: 12px; display: flex; align-items: center; justify-content: center;
      font-size: 1.2rem; color: var(--accent2);
    }
    .mode-card h2 { font-family: var(--display); font-size: 1.05rem; font-weight: 700; margin-bottom: 0.5rem; }
    .mode-card p { font-size: 0.87rem; color: var(--muted); line-height: 1.7; }

    /* === FEATURES BENTO GRID === */
    .bento {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      grid-template-rows: auto auto;
      gap: 1rem;
      margin-top: 2.5rem;
    }
    .bento-card { padding: clamp(1.2rem, 2.5vw, 1.8rem); }
    .bento-card.wide { grid-column: span 2; }
    .bento-icon {
      width: 40px; height: 40px; border-radius: 10px;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.1rem; margin-bottom: 1rem;
    }
    .bento-card h3 { font-family: var(--display); font-size: 1.02rem; font-weight: 700; margin-bottom: 0.5rem; }
    .bento-card p { font-size: 0.85rem; color: var(--muted); line-height: 1.7; }
    .bento-card .stat { font-family: var(--display); font-size: 2rem; font-weight: 700; color: var(--accent2); line-height: 1; margin-bottom: 0.3rem; }
    .bento-card .stat-label { font-size: 0.82rem; color: var(--muted); }

    /* === STEPS === */
    .steps-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1.2rem; margin-top: 2.5rem; }
    .step-card { text-align: center; padding: 2rem 1.5rem; }
    .step-num {
      width: 52px; height: 52px; margin: 0 auto 1.2rem;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      border-radius: 14px; display: flex; align-items: center; justify-content: center;
      font-family: var(--display); font-size: 1.4rem; font-weight: 700; color: #fff;
      box-shadow: 0 8px 24px rgba(124,109,250,0.35);
    }
    .step-card h3 { font-family: var(--display); font-size: 1.05rem; margin-bottom: 0.6rem; }
    .step-card p { font-size: 0.87rem; color: var(--muted); line-height: 1.7; }

    /* === WHY GRID === */
    .why-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1rem; margin-top: 2.5rem; }
    .why-card { padding: 1.4rem 1.5rem; display: flex; gap: 12px; align-items: flex-start; }
    .why-emoji { font-size: 1.4rem; flex-shrink: 0; line-height: 1.3; }
    .why-card h4 { font-family: var(--display); font-size: 0.95rem; font-weight: 700; margin-bottom: 0.25rem; }
    .why-card p { font-size: 0.83rem; color: var(--muted); line-height: 1.6; }

    /* === COMPARISON TABLE === */
    .compare-wrap { margin-top: 2.5rem; overflow-x: auto; }
    .compare-table { width: 100%; border-collapse: collapse; font-size: 0.9rem; }
    .compare-table th, .compare-table td {
      padding: 0.9rem 1.2rem; text-align: left; border-bottom: 1px solid var(--border);
    }
    .compare-table th { font-family: var(--display); font-weight: 700; font-size: 0.85rem; color: var(--muted); text-transform: uppercase; letter-spacing: 0.06em; }
    .compare-table td:first-child { color: var(--text); font-weight: 500; }
    .compare-table tr:last-child td { border-bottom: none; }
    .compare-table tr:hover td { background: var(--surface); }
    .tick { color: var(--accent3); font-size: 1.1rem; }
    .cross { color: #fc6b6b; font-size: 1.1rem; }
    .us-col { color: var(--accent2); font-weight: 600; }

    /* === TALK SECTION === */
    .talk-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.2rem; margin-top: 2.5rem; }
    .talk-card h3 { font-family: var(--display); font-size: 1rem; font-weight: 700; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 8px; }
    .talk-card h3 .ti { font-size: 1.2rem; }
    .talk-card p { font-size: 0.87rem; color: var(--muted); line-height: 1.75; }
    .talk-summary {
      margin-top: 1.5rem; padding: 1.5rem 2rem;
      background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius);
      font-size: 0.9rem; color: var(--muted); line-height: 1.85;
    }
    .talk-summary strong { color: var(--text); }
    .talk-summary a { color: var(--accent2); text-decoration: underline; }

    /* === SAFETY GRID === */
    .safety-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 0.8rem; margin-top: 2.5rem; }
    .safety-item {
      display: flex; align-items: center; gap: 10px;
      padding: 0.9rem 1.2rem; background: var(--surface);
      border: 1px solid var(--border); border-radius: var(--radius-sm);
      font-size: 0.875rem; font-weight: 500;
    }
    .safety-item .si { color: var(--accent3); font-size: 1rem; flex-shrink: 0; }
    .safety-tip { margin-top: 1.2rem; text-align: center; font-size: 0.8rem; color: var(--muted); }

    /* === FAQ === */
    .faq-list { max-width: 760px; margin: 2.5rem auto 0; }
    .faq-item {
      border-bottom: 1px solid var(--border);
      transition: background 0.2s;
    }
    .faq-item:last-child { border-bottom: none; }
    .faq-q {
      width: 100%; background: none; border: none; text-align: left;
      padding: 1.2rem 0; cursor: pointer;
      display: flex; justify-content: space-between; align-items: center; gap: 12px;
      font-family: var(--body); font-size: 0.97rem; font-weight: 600; color: var(--text);
    }
    .faq-q:hover { color: var(--accent2); }
    .faq-chevron { flex-shrink: 0; transition: transform 0.3s; font-size: 0.85rem; color: var(--muted); }
    .faq-item.open .faq-chevron { transform: rotate(180deg); }
    .faq-a {
      max-height: 0; overflow: hidden;
      transition: max-height 0.35s ease, padding 0.35s;
      font-size: 0.9rem; color: var(--muted); line-height: 1.8; padding: 0;
    }
    .faq-item.open .faq-a { max-height: 300px; padding-bottom: 1.2rem; }

    /* === CTA BANNER === */
    .cta-banner {
      margin: 0 0 3rem;
      background: linear-gradient(135deg, rgba(124,109,250,0.12), rgba(56,217,169,0.08));
      border: 1px solid rgba(124,109,250,0.2);
      border-radius: 20px; padding: clamp(2rem, 5vw, 3.5rem) 2rem;
      text-align: center;
      position: relative; overflow: hidden;
    }
    .cta-banner::before {
      content: ''; position: absolute; inset: 0;
      background: radial-gradient(ellipse at 50% 0%, rgba(124,109,250,0.15), transparent 70%);
      pointer-events: none;
    }
    .cta-banner h2 { font-family: var(--display); font-size: clamp(1.5rem, 3.5vw, 2.2rem); margin-bottom: 0.6rem; }
    .cta-banner p { color: var(--muted); margin-bottom: 1.8rem; font-size: 1rem; }
    .cta-note { margin-top: 1rem; font-size: 0.78rem; color: var(--muted); }

    /* === FOOTER === */
    footer {
      position: relative; z-index: 1;
      border-top: 1px solid var(--border);
      padding: 2rem 0;
    }
    .footer-inner {
      display: flex; justify-content: space-between; align-items: center;
      flex-wrap: wrap; gap: 1rem;
    }
    .footer-links { display: flex; flex-wrap: wrap; gap: 0 1.4rem; }
    .footer-links a {
      font-size: 0.875rem; color: var(--muted); text-decoration: none;
      transition: color 0.2s;
    }
    .footer-links a:hover { color: var(--accent2); }
    footer p.copy { font-size: 0.8rem; color: var(--muted); }

    /* === FADE-IN REVEAL === */
    .reveal { opacity: 0; transform: translateY(24px); transition: opacity 0.6s ease, transform 0.6s ease; }
    .reveal.in { opacity: 1; transform: translateY(0); }

    /* === KEYFRAMES === */
    @keyframes fadeUp { from { opacity:0; transform:translateY(20px); } to { opacity:1; transform:none; } }
    @keyframes blink { 0%,100% { opacity:1; } 50% { opacity:0.2; } }
    @keyframes pulse-glow {
      0%,100% { box-shadow: 0 0 0 0 rgba(124,109,250,0.4); }
      50% { box-shadow: 0 0 0 8px rgba(124,109,250,0); }
    }

    /* === MOBILE NAV DRAWER === */
    .nav-overlay {
      display: none; position: fixed; inset: 0;
      background: rgba(0,0,0,0.6); z-index: 99;
    }
    .nav-overlay.show { display: block; }

    /* === RESPONSIVE === */
    @media (max-width: 900px) {
      .bento { grid-template-columns: 1fr 1fr; }
      .bento-card.wide { grid-column: span 2; }
      .why-grid { grid-template-columns: 1fr 1fr; }
      .safety-grid { grid-template-columns: 1fr 1fr; }
      .steps-grid { grid-template-columns: 1fr 1fr; }
    }

    @media (max-width: 700px) {
      .nav-links {
        position: fixed; top: 0; left: -100%; width: 78%; max-width: 290px;
        height: 100vh; flex-direction: column; justify-content: center; align-items: flex-start;
        background: rgba(10,10,15,0.98); padding: 2rem 1.8rem; gap: 0.3rem;
        z-index: 100; transition: left 0.35s ease;
      }
      .nav-links.open { left: 0; }
      .nav-links a { padding: 0.8rem 0.5rem; font-size: 1rem; }
      .nav-cta { display: inline-flex !important; }
      .mobile-btn { display: block; }

      .modes-grid, .talk-grid { grid-template-columns: 1fr; }
      .bento { grid-template-columns: 1fr; }
      .bento-card.wide { grid-column: span 1; }
      .why-grid { grid-template-columns: 1fr; }
      .steps-grid { grid-template-columns: 1fr; }
      .safety-grid { grid-template-columns: 1fr 1fr; }
      .footer-inner { flex-direction: column; text-align: center; }
      .footer-links { justify-content: center; }
    }

    @media (max-width: 480px) {
      .safety-grid { grid-template-columns: 1fr; }
      .cta-group { flex-direction: column; align-items: center; }
      .btn { width: 100%; max-width: 300px; justify-content: center; }
    }

    /* === SKIP LINK === */
    .skip { position: absolute; top: -50px; left: 0; z-index: 9999; background: var(--accent); color: #fff; padding: 8px 14px; font-size: 0.9rem; border-radius: 0 0 8px 0; transition: top 0.2s; }
    .skip:focus { top: 0; }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
    }
  </style>

  <!-- ✅ Google Analytics — deferred, non-blocking -->
  <script>
    window.addEventListener('load', function() {
      var s = document.createElement('script');
      s.async = true;
      s.src = 'https://www.googletagmanager.com/gtag/js?id=G-V92WH20EPP';
      document.head.appendChild(s);
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-V92WH20EPP', { send_page_view: true });
    });
  </script>

  <!-- ✅ AdSense — deferred -->
  <script>
    window.addEventListener('load', function() {
      var s = document.createElement('script');
      s.async = true;
      s.src = 'https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4169258087382172';
      s.setAttribute('crossorigin','anonymous');
      document.head.appendChild(s);
    });
  </script>
</head>

<body>
<a class="skip" href="#main">Skip to main content</a>
<div class="orbs" aria-hidden="true">
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
</div>
<div class="nav-overlay" id="overlay"></div>

<!-- ===================== NAV ===================== -->
<nav id="nav" aria-label="Main navigation">
  <a href="/" class="nav-logo" title="ChatWithStrangers – Free Random Chat Home">
    <img src="/logo.png" width="110" height="36" alt="ChatWithStrangers.fun logo" loading="eager" decoding="async">
    <span class="nav-wordmark">Chat<span>Strangers</span></span>
  </a>
  <button class="mobile-btn" id="menuBtn" aria-label="Open navigation menu" aria-expanded="false" aria-controls="navLinks">&#9776;</button>
  <ul class="nav-links" id="navLinks" role="menubar" aria-label="Main menu">
    <li role="none"><a href="/" role="menuitem" class="active" aria-current="page">Home</a></li>
    <li role="none"><a href="/about/" role="menuitem">About</a></li>
    <li role="none"><a href="/blog/best-omegle-alternatives/" role="menuitem">Blog</a></li>
    <li role="none"><a href="/contact/" role="menuitem">Contact</a></li>
    <li role="none"><a href="/text-chat" role="menuitem" class="nav-cta">Start Chatting →</a></li>
  </ul>
</nav>

<!-- ===================== MAIN ===================== -->
<main id="main">

  <!-- ════════════════════════════════════════
       HERO
  ════════════════════════════════════════ -->
  <section class="hero" aria-labelledby="h1">

    <div class="hero-eyebrow">
      <span class="live-dot"></span>
      <span id="liveCount" aria-live="polite" aria-atomic="true">5,284</span> people chatting right now
    </div>

    <h1 id="h1">
      <span class="line-plain">Chat with Strangers Online —</span>
      <span class="line-grad">Free Random Video &amp; Text Chat</span>
    </h1>

    <p class="hero-sub">
      Instant, anonymous connections with real people from 190+ countries.
      No sign‑up. No download. No waiting. The best <strong>Omegle alternative</strong> in 2026 — start in 5 seconds.
    </p>

    <p class="hero-updated">Last updated: <time datetime="2026-06-17">June 17, 2026</time></p>

    <div class="trust-row" aria-label="Platform highlights">
      <span class="pill"><span class="dot-green"></span> 190+ Countries</span>
      <span class="pill">⚡ Matched in 3 seconds</span>
      <span class="pill">🔒 100% Anonymous</span>
      <span class="pill">✅ Always Free</span>
      <span class="pill">📱 Works on Mobile</span>
    </div>

    <div class="cta-group">
      <button id="startVideo" class="btn btn-primary">
        <i class="fas fa-video" aria-hidden="true"></i> Start Video Chat — Free
      </button>
      <a href="/text-chat" class="btn btn-ghost" aria-label="Start anonymous text chat with strangers online">
        <i class="fas fa-comment-dots" aria-hidden="true"></i> Text Chat Instead
      </a>
    </div>

    <p class="hero-proof">
      Trusted by users from <strong>190+ countries</strong> &nbsp;·&nbsp; <strong>4.8★</strong> rated by 12,000+ users &nbsp;·&nbsp; No registration ever
    </p>

  </section>

  <!-- ════════════════════════════════════════
       CHAT MODES
  ════════════════════════════════════════ -->
  <div class="wrap">
    <div class="divider"></div>
  </div>

  <section class="sec reveal" aria-labelledby="modes-h2">
    <div class="wrap">
      <span class="sec-label">Two ways to connect</span>
      <h2 id="modes-h2" class="sec-title">Random Video Chat &amp; Anonymous Text Chat</h2>
      <p class="sec-body">
        Whether you want face-to-face video or prefer to stay behind the keyboard — both modes are 100% free, instant, and completely anonymous.
      </p>
      <div class="modes-grid">
        <article class="card mode-card">
          <div class="mode-icon"><i class="fas fa-video" aria-hidden="true"></i></div>
          <div>
            <h2>Random Video Chat with Strangers</h2>
            <p>
              Go live with a random stranger from anywhere on earth — face-to-face, no account needed. Our <strong>random video chat</strong> connects you in under 3 seconds. Perfect for making new friends, practicing languages, or a spontaneous conversation you'll never forget.
            </p>
          </div>
        </article>
        <article class="card mode-card">
          <div class="mode-icon"><i class="fas fa-comment-dots" aria-hidden="true"></i></div>
          <div>
            <h2>Anonymous Text Chat with Strangers</h2>
            <p>
              Stay private, camera off. Our <strong>anonymous text chat</strong> lets you talk to strangers online with zero personal info shared — no name, no email, nothing. Type freely and switch to video any time you feel ready.
            </p>
          </div>
        </article>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       FEATURES BENTO GRID
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="features-h2">
    <div class="wrap">
      <div class="centered">
        <span class="sec-label">Platform features</span>
        <h2 id="features-h2" class="sec-title">Why Millions Choose ChatWithStrangers</h2>
        <p class="sec-body">A faster, safer, and truly free random chat experience — built for real human connection.</p>
      </div>

      <div class="bento">
        <article class="card bento-card">
          <div class="bento-icon" style="background:rgba(56,217,169,0.12);color:var(--accent3);">🌍</div>
          <div class="stat">190+</div>
          <div class="stat-label">Countries connected</div>
          <p style="margin-top:0.6rem;">Meet strangers from every corner of the globe in real time — pure cultural exchange at zero cost.</p>
        </article>

        <article class="card bento-card wide">
          <div class="bento-icon" style="background:rgba(124,109,250,0.12);color:var(--accent2);"><i class="fas fa-shield-alt" aria-hidden="true"></i></div>
          <h3>Anonymous &amp; Privacy-First</h3>
          <p>No email, no phone, no personal data — ever. Active 24/7 moderation ensures a respectful, safe environment. Your identity stays protected in every conversation, every time.</p>
        </article>

        <article class="card bento-card">
          <div class="bento-icon" style="background:rgba(252,107,107,0.1);color:#fc6b6b;"><i class="fas fa-bolt" aria-hidden="true"></i></div>
          <h3>Matched in Under 3 Seconds</h3>
          <p>No queues. No waiting rooms. Click Start and you're talking to someone new from anywhere on the planet instantly.</p>
        </article>

        <article class="card bento-card">
          <div class="bento-icon" style="background:rgba(56,217,169,0.1);color:var(--accent3);"><i class="fas fa-mobile-alt" aria-hidden="true"></i></div>
          <h3>Works on Any Device</h3>
          <p>Desktop, tablet, iOS, Android — no download needed. Open your browser and start talking. It just works.</p>
        </article>

        <article class="card bento-card">
          <div class="bento-icon" style="background:rgba(124,109,250,0.12);color:var(--accent2);"><i class="fas fa-sliders-h" aria-hidden="true"></i></div>
          <h3>Gender &amp; Interest Filters</h3>
          <p>Use filters to find strangers who match your interests or preferred gender — smarter, more meaningful connections.</p>
        </article>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       HOW IT WORKS — 3 steps
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="steps-h2">
    <div class="wrap">
      <div class="centered">
        <span class="sec-label">Quick start</span>
        <h2 id="steps-h2" class="sec-title">How to Chat with Strangers in 3 Steps</h2>
        <p class="sec-body centered">No downloads. No accounts. No waiting. Start your random chat adventure in under 5 seconds.</p>
      </div>
      <div class="steps-grid">
        <article class="card step-card">
          <div class="step-num" aria-label="Step 1">1</div>
          <h3>Open &amp; Click Start</h3>
          <p>Open <strong>chatwithstrangers.fun</strong> in any browser on any device. Click the Start button. No registration, no download — done in 5 seconds.</p>
        </article>
        <article class="card step-card">
          <div class="step-num" aria-label="Step 2">2</div>
          <h3>Get Matched Instantly</h3>
          <p>Our algorithm pairs you with a random stranger from 190+ countries in under 3 seconds — ready for <strong>video</strong> or <strong>text chat</strong>, your choice.</p>
        </article>
        <article class="card step-card">
          <div class="step-num" aria-label="Step 3">3</div>
          <h3>Chat, Skip &amp; Repeat</h3>
          <p>Enjoy the conversation as long as you like. Not feeling it? One click skips to someone new. Thousands are online right now.</p>
        </article>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       WHY US — Trust signals
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="why-h2">
    <div class="wrap">
      <span class="sec-label">What makes us different</span>
      <h2 id="why-h2" class="sec-title">More Than a Random Chat Site</h2>
      <p class="sec-body">Designed with your safety, privacy, and experience as the only priorities.</p>
      <div class="why-grid">
        <div class="card why-card">
          <span class="why-emoji">🆓</span>
          <div>
            <h4>Always 100% Free</h4>
            <p>No subscriptions, credits, or premium tiers. Every feature is free — forever, no fine print.</p>
          </div>
        </div>
        <div class="card why-card">
          <span class="why-emoji">📹</span>
          <div>
            <h4>HD Video via WebRTC</h4>
            <p>Crystal-clear video powered by WebRTC. Smooth, lag-free random video chat on any connection.</p>
          </div>
        </div>
        <div class="card why-card">
          <span class="why-emoji">🚀</span>
          <div>
            <h4>Skip Anytime</h4>
            <p>One click skips to a new stranger. Total control over who you talk to, always.</p>
          </div>
        </div>
        <div class="card why-card">
          <span class="why-emoji">🔒</span>
          <div>
            <h4>Zero Data Collected</h4>
            <p>We collect no personal data. No name, email, location — nothing. Your anonymity is absolute.</p>
          </div>
        </div>
        <div class="card why-card">
          <span class="why-emoji">🌐</span>
          <div>
            <h4>190+ Countries</h4>
            <p>Connect with real people from every continent in real time. The world is one click away.</p>
          </div>
        </div>
        <div class="card why-card">
          <span class="why-emoji">🔄</span>
          <div>
            <h4>Video + Text, Anytime</h4>
            <p>Switch seamlessly between random video chat and anonymous text chat within the same session.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       COMPARISON TABLE — Omegle Alternative SEO
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="compare-h2">
    <div class="wrap">
      <div class="centered">
        <span class="sec-label">vs. the competition</span>
        <h2 id="compare-h2" class="sec-title">Best Omegle Alternative in 2026</h2>
        <p class="sec-body">Since Omegle shut down, millions searched for a better replacement. Here's how ChatWithStrangers compares to other random chat sites.</p>
      </div>
      <div class="compare-wrap">
        <table class="compare-table" aria-label="Comparison of random chat platforms">
          <thead>
            <tr>
              <th>Feature</th>
              <th class="us-col">ChatWithStrangers ✦</th>
              <th>Chatroulette</th>
              <th>OmeTV</th>
              <th>Emerald Chat</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Completely Free</td>
              <td class="us-col tick">✔</td>
              <td class="tick">✔</td>
              <td class="cross">✘ Paid features</td>
              <td class="cross">✘ Paid features</td>
            </tr>
            <tr>
              <td>No Registration</td>
              <td class="us-col tick">✔</td>
              <td class="tick">✔</td>
              <td class="cross">✘ Required</td>
              <td class="cross">✘ Required</td>
            </tr>
            <tr>
              <td>Text + Video</td>
              <td class="us-col tick">✔ Both</td>
              <td class="tick">✔ Video only</td>
              <td class="tick">✔ Both</td>
              <td class="tick">✔ Both</td>
            </tr>
            <tr>
              <td>24/7 Moderation</td>
              <td class="us-col tick">✔ Active</td>
              <td class="cross">✘ Limited</td>
              <td class="tick">✔</td>
              <td class="tick">✔</td>
            </tr>
            <tr>
              <td>Mobile Browser</td>
              <td class="us-col tick">✔ Perfect</td>
              <td class="tick">✔</td>
              <td class="cross">✘ App required</td>
              <td class="tick">✔</td>
            </tr>
            <tr>
              <td>Zero Data Collected</td>
              <td class="us-col tick">✔</td>
              <td class="cross">✘</td>
              <td class="cross">✘</td>
              <td class="cross">✘</td>
            </tr>
          </tbody>
        </table>
      </div>
      <p style="margin-top:1rem;font-size:0.82rem;color:var(--muted);text-align:center;">
        Want the full breakdown? Read our <a href="/blog/best-omegle-alternatives/" style="color:var(--accent2);text-decoration:underline;">guide to the best Omegle alternatives in 2026</a>.
      </p>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       TALK WITH STRANGERS — Keyword cluster
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="talk-h2">
    <div class="wrap">
      <span class="sec-label">Talk with strangers online</span>
      <h2 id="talk-h2" class="sec-title">Talk with Strangers — Free, Safe &amp; Instant</h2>
      <p class="sec-body">
        Want to <strong>talk with strangers</strong> without sign-up, payment, or hassle? We make it simple — connect with a real person anywhere in the world in under 5 seconds.
      </p>
      <div class="talk-grid">
        <article class="card talk-card">
          <h3><span class="ti">🎙️</span> Talk Face-to-Face via Video</h3>
          <p>Our random video chat lets you <strong>talk with strangers</strong> live using your webcam. Hear real voices, see real expressions, and have genuine conversations with people from the US, UK, India, Australia, Canada, and 190+ other countries — completely free.</p>
        </article>
        <article class="card talk-card">
          <h3><span class="ti">⌨️</span> Talk via Anonymous Text</h3>
          <p>Not comfortable on camera? <strong>Talk with strangers</strong> through anonymous text chat. Stay fully private — no webcam, no microphone, no identity. Type freely and enjoy real conversations on your own terms.</p>
        </article>
        <article class="card talk-card">
          <h3><span class="ti">🌍</span> Talk with People from Any Country</h3>
          <p>Break geographical barriers and <strong>talk with strangers</strong> from different cultures and backgrounds. Practice English, learn about life in other countries, or enjoy an unexpected conversation with someone you'd never normally meet.</p>
        </article>
        <article class="card talk-card">
          <h3><span class="ti">🔒</span> Talk Anonymously — Zero Personal Info</h3>
          <p>When you <strong>talk with strangers</strong> here, you never reveal who you are. No name, no email, no phone. Just open the site, click Start, and talk instantly. Full anonymity, zero data collection, always.</p>
        </article>
      </div>
      <div class="talk-summary">
        <p>ChatWithStrangers is built for anyone who wants to <strong>talk with strangers online</strong> in a safe, fun, and completely free environment. Unlike social media where you only talk to people you already know, our platform creates spontaneous, real-time connections every day. Whether you're bored at 2 AM, want to improve social confidence, practice a language, or simply hear a fresh perspective — click Start and connect. Thousands of strangers are online right now. Also see our <a href="/blog/best-omegle-alternatives/">list of the best random chat sites in 2026</a> for more options.</p>
      </div>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       SAFETY
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="safety-h2">
    <div class="wrap">
      <div class="centered">
        <span class="sec-label">Your safety</span>
        <h2 id="safety-h2" class="sec-title">Your Safety Is Our Priority</h2>
        <p class="sec-body">Multiple layers of protection so you can chat with strangers comfortably and confidently.</p>
      </div>
      <div class="safety-grid">
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> Active content moderation 24/7</div>
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> One-click skip &amp; block any user</div>
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> Zero personal data collected</div>
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> Report abusive users instantly</div>
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> No login or email ever required</div>
        <div class="safety-item"><span class="si"><i class="fas fa-check-circle" aria-hidden="true"></i></span> Encrypted HTTPS connections</div>
      </div>
      <p class="safety-tip">⚠️ Tip: Never share your real name, phone number, or home location with strangers online.</p>
    </div>
  </section>

  <div class="wrap"><div class="divider"></div></div>

  <!-- ════════════════════════════════════════
       FAQ — Matches schema exactly
  ════════════════════════════════════════ -->
  <section class="sec reveal" aria-labelledby="faq-h2">
    <div class="wrap">
      <div class="centered">
        <span class="sec-label">FAQs</span>
        <h2 id="faq-h2" class="sec-title">Frequently Asked Questions</h2>
      </div>
      <div class="faq-list" role="list">

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Is ChatWithStrangers completely free to use?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Yes — 100% free, always. No hidden charges, premium plans, credits, or subscriptions. Every feature — random video chat, anonymous text chat, instant matching, and filters — is available at zero cost, forever.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Do I need to register or create an account?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">No registration required. No email, phone number, or personal information needed. Open the website, click Start, and you'll be connected with a random stranger in seconds.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Can I use both video chat and text chat?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Yes — freely switch between live random video chat and anonymous text chat. Both modes work on all devices with no restrictions. You can switch between conversations without any limits.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Is ChatWithStrangers the best Omegle alternative in 2026?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Yes — one of the best Omegle alternatives in 2026. After Omegle shut down, ChatWithStrangers became the most trusted platform for random chat with faster matching, stronger safety features, and better privacy protection than any competitor.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Is it safe to talk with strangers online here?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Yes. We use 24/7 active content moderation, one-click skip and report, HTTPS encryption, and collect zero personal data. You never need to share your name, email, or location. Always exercise general online safety — never share financial details with anyone online.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Does ChatWithStrangers work on mobile phones?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Yes, perfectly. Works on all modern iOS and Android smartphones and tablets directly in your mobile browser (Chrome, Safari, Firefox). No app download required — just open and start chatting.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            Why do people like to talk with strangers online?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Random stranger chat offers something social media can't — spontaneous, judgment-free conversation with someone completely new. People use it to fight boredom, build social confidence, practice languages, get outside perspectives, or simply enjoy an unexpected conversation. Every connection is a surprise.</div>
        </div>

        <div class="faq-item" role="listitem">
          <button class="faq-q" aria-expanded="false">
            How many people are online right now?
            <i class="fas fa-chevron-down faq-chevron" aria-hidden="true"></i>
          </button>
          <div class="faq-a">Thousands of users from 190+ countries are active at any moment. You can see the real-time count at the top of the page. You're never more than seconds away from meeting someone new.</div>
        </div>

      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       FINAL CTA BANNER
  ════════════════════════════════════════ -->
  <div class="wrap">
    <div class="cta-banner reveal" aria-label="Call to action — start chatting now">
      <h2>Ready to Meet Someone New?</h2>
      <p>Join <strong>thousands of people online right now</strong> — your next interesting conversation is one click away.</p>
      <div class="cta-group">
        <button id="startVideo2" class="btn btn-primary">
          <i class="fas fa-play" aria-hidden="true"></i> Start Chatting — It's Free
        </button>
        <a href="/text-chat" class="btn btn-ghost" aria-label="Start anonymous text chat">
          <i class="fas fa-comment-dots" aria-hidden="true"></i> Text Chat Instead
        </a>
      </div>
      <p class="cta-note">No registration &nbsp;·&nbsp; No downloads &nbsp;·&nbsp; 100% Anonymous &nbsp;·&nbsp; Forever Free</p>
    </div>
  </div>

</main>

<!-- ===================== FOOTER ===================== -->
<footer>
  <div class="wrap">
    <div class="footer-inner">
      <nav class="footer-links" aria-label="Footer navigation">
        <a href="/legal/" title="Terms of Service and Privacy Policy">Terms &amp; Privacy</a>
        <a href="/about/" title="About ChatWithStrangers">About Us</a>
        <a href="/contact/" title="Contact ChatWithStrangers">Contact</a>
        <a href="/blog/best-omegle-alternatives/" title="Best Omegle Alternatives 2026">Blog</a>
      </nav>
      <p class="copy">© 2026 <strong>ChatWithStrangers.fun</strong> — Free Random Video &amp; Text Chat Online</p>
    </div>
  </div>
</footer>

<!-- ✅ Socket.io — loaded asynchronously, with onload/onerror handlers -->
<script async 
        src="/socket.io/socket.io.js" 
        onload="initSocket()" 
        onerror="handleSocketError()">
</script>

<script>
/* ── Global socket initialization (called only when socket.io loads) ── */
function initSocket() {
  if (typeof io === 'undefined') return;

  var socket = io();
  var liveEl = document.getElementById('liveCount');

  socket.on('updateUserCount', function (count) {
    if (liveEl) liveEl.textContent = Number(count).toLocaleString();
  });

  socket.on('waiting', function () {
    document.querySelectorAll('#startVideo, #startVideo2').forEach(function (btn) {
      btn.innerHTML = '<i class="fas fa-user-clock" aria-hidden="true"></i> Waiting for someone…';
      btn.disabled = true;
    });
  });

  socket.on('start-chat', function (roomId) {
    window.location.href = '/chat?room=' + roomId;
  });

  function startVideo(btn) {
    btn.innerHTML = '<i class="fas fa-spinner fa-spin" aria-hidden="true"></i> Finding someone…';
    btn.disabled = true;
    socket.emit('join-queue');
  }

  document.getElementById('startVideo').addEventListener('click', function () { startVideo(this); });
  document.getElementById('startVideo2').addEventListener('click', function () { startVideo(this); });
}

/* ── Fallback if socket.io script fails to load ── */
function handleSocketError() {
  console.warn('Socket.IO failed to load. Video chat unavailable.');

  var msg = '<i class="fas fa-exclamation-triangle" aria-hidden="true"></i> Service Unavailable';
  document.querySelectorAll('#startVideo, #startVideo2').forEach(function (btn) {
    btn.innerHTML = msg;
    btn.disabled = true;
    btn.style.opacity = '0.6';
    btn.style.cursor = 'not-allowed';
  });
}
</script>

<script>
(function () {
  'use strict';

  /* ── Mobile Nav ── */
  var menuBtn = document.getElementById('menuBtn');
  var navLinks = document.getElementById('navLinks');
  var overlay = document.getElementById('overlay');
  var nav = document.getElementById('nav');

  function openNav() {
    navLinks.classList.add('open');
    overlay.classList.add('show');
    menuBtn.setAttribute('aria-expanded', 'true');
    menuBtn.setAttribute('aria-label', 'Close navigation menu');
    menuBtn.innerHTML = '&#10005;';
    document.body.style.overflow = 'hidden';
  }
  function closeNav() {
    navLinks.classList.remove('open');
    overlay.classList.remove('show');
    menuBtn.setAttribute('aria-expanded', 'false');
    menuBtn.setAttribute('aria-label', 'Open navigation menu');
    menuBtn.innerHTML = '&#9776;';
    document.body.style.overflow = '';
  }

  menuBtn.addEventListener('click', function () {
    navLinks.classList.contains('open') ? closeNav() : openNav();
  });
  overlay.addEventListener('click', closeNav);
  navLinks.querySelectorAll('a').forEach(function (a) { a.addEventListener('click', closeNav); });

  /* ── Sticky nav ── */
  var solid = false;
  window.addEventListener('scroll', function () {
    var s = window.scrollY > 40;
    if (s !== solid) { nav.classList.toggle('solid', s); solid = s; }
  }, { passive: true });

  /* ── Scroll reveal ── */
  if ('IntersectionObserver' in window) {
    var io = new IntersectionObserver(function (entries) {
      entries.forEach(function (e) {
        if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); }
      });
    }, { threshold: 0.07, rootMargin: '0px 0px -40px 0px' });
    document.querySelectorAll('.reveal').forEach(function (el) { io.observe(el); });
  } else {
    document.querySelectorAll('.reveal').forEach(function (el) { el.classList.add('in'); });
  }

  /* ── FAQ Accordion ── */
  document.querySelectorAll('.faq-q').forEach(function (btn) {
    btn.addEventListener('click', function () {
      var item = btn.closest('.faq-item');
      var open = item.classList.contains('open');

      document.querySelectorAll('.faq-item').forEach(function (i) {
        i.classList.remove('open');
        i.querySelector('.faq-q').setAttribute('aria-expanded', 'false');
      });

      if (!open) {
        item.classList.add('open');
        btn.setAttribute('aria-expanded', 'true');
      }
    });
    btn.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); btn.click(); }
    });
  });

})();
</script>

</body>
</html>
